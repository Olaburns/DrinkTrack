# Database Configuration
# For local development with Docker: use the connection string below
# For Replit: this is automatically provided, no need to set
DATABASE_URL=postgresql://drinktracker:devpassword@localhost:5432/drinktracker

# Server Configuration
PORT=5000

# Snapshot Settings (optional)
SNAPSHOT_INTERVAL_SEC=120
SNAPSHOT_DIR=./snapshots
SNAPSHOT_MAX_FILES=30

# Session Secret (optional - will be auto-generated if not provided)
# SESSION_SECRET=your-secret-key-here
